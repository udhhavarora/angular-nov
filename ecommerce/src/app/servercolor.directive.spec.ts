import { ServercolorDirective } from './servercolor.directive';

describe('ServercolorDirective', () => {
  it('should create an instance', () => {
    const directive = new ServercolorDirective();
    expect(directive).toBeTruthy();
  });
});
